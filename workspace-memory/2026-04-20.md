## 2026-04-20 工作记录

### WorkBuddy 多设备同步配置

在 `~/.workbuddy/` 建立了 Git 仓库，推送到 GitHub：
- 仓库地址：git@github.com:gyjiang/workbuddy-memory.git
- 同步范围：skills（22个）+ SOUL/IDENTITY/USER + workspace-memory
- 自动化脚本：~/.workbuddy/wb-sync.sh

本机 workspace memory 路径：`/Users/guoyou/WorkBuddy/20260420100117/.workbuddy/memory`

**家里两台电脑配置步骤：**
```bash
# 1. 克隆仓库
git clone git@github.com:gyjiang/workbuddy-memory.git ~/.workbuddy

# 2. 配置工作区路径（自动检测，如需手动指定用 setup）
cd ~/.workbuddy && ./wb-sync.sh setup

# 3. 同步（pull + push 双向）
cd ~/.workbuddy && ./wb-sync.sh
```

配置文件：~/.workbuddy/.wb-sync-config（不纳入 Git 同步，每台机器独立）
